{
  "name": "translator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "npx --yes vercel@54.2.0 dev",
    "local": "npm run build && node server.cjs",
    "test": "node --test test/*.test.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
